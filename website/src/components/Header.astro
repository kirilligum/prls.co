---
interface Props {
  title?: string;
}

const { title = "Pearls of Wisdom" } = Astro.props;
---

<div class="main-header">
  <span class="header-pearl text-nowrap">
    {title === "Pearls of Wisdom" ? <a href="/" class="home-link-style">{title}</a> : title}
  </span>
  <div class="header-content">
    <span class="header-links">
      <a href="/" class="header-link">Home</a>
      <a href="/about/" class="header-link">About</a>
      <a href="/blog/" class="header-link">Blog</a>
    </span>
  </div>
</div>

<style>
  .main-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0rem 1rem;
    padding-top: 0.5rem;
    height: 2rem;
    background-color: var(--hero-gradient-start);
  }

  .header-pearl {
    font-family: var(--font-heading);
    font-size: 1.5em;
    font-weight: 500;
    margin-top: -1em;
    margin-bottom: -0.7em;
    color: var(--hf-color);
  }

  .header-symbol {
    font-size: 1.5em;
    position: relative;
    top: -0.12em;
    margin-right: 0.25em;
  }

  .header-content {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 1rem;
  }

  .header-links {
    text-align: right;
  }

  .home-link-style {
    text-decoration: none;
    color: inherit;
  }

  .home-link-style:hover {
    text-decoration: underline;
  }
  .header-link {
    margin-left: 1rem;
    text-decoration: none;
    color: inherit;
  }
  .header-link:hover {
    text-decoration: underline;
  }
</style>
